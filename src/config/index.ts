/**
 * Configuration exports
 *
 * Usage:
 * - Client: import { clientConfig } from '@/config'
 * - Server: import { serverConfig } from '@/config/server'
 *
 * Supabase: Use environment variables directly via @/utils/supabase/*
 */

export { clientConfig } from './client';
